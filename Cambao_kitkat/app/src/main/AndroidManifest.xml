<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="br.edu.insper.al.carolineclc.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DetailsFrete"></activity>
        <activity android:name=".Perfil" />
        <activity android:name=".AcceptDelivery" />
        <activity android:name=".Caixa" />
        <activity android:name=".Fretes" />
        <activity android:name=".NovoCadastro" />
        <activity android:name=".Contato" />
        <activity android:name=".Deliveries" />
        <activity android:name=".MainScreen" />
        <activity android:name=".Info" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>